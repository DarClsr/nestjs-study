import { QuerypageInterceptor } from './querypage.interceptor';

describe('QuerypageInterceptor', () => {
  it('should be defined', () => {
    expect(new QuerypageInterceptor()).toBeDefined();
  });
});
